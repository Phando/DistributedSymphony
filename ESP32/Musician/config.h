#ifndef CONFIG_H
#define CONFIG_H

#include "Arduino.h"

//#define max(a,b) ((a)>(b)?(a):(b)) 
//#define min(a,b) ((a)<(b)?(a):(b))

#define NOTE_PIN 26
#define MIC_PIN 23
#define ENABLED 0 
#define DISABLED 1

#endif
